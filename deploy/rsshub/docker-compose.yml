services:
  rsshub:
    image: diygod/rsshub:latest
    restart: always
    ports:
      - "1200:1200"
    environment:
      NODE_ENV: production
      CACHE_TYPE: redis
      REDIS_URL: "redis://redis:6379/"
      PUPPETEER_WS_ENDPOINT: "ws://browserless:3000"
      # X / Twitter access. Fill TWITTER_AUTH_TOKEN in deploy/rsshub/.env
      # (the auth_token cookie value from a logged-in — ideally throwaway — X account).
      TWITTER_AUTH_TOKEN: "${TWITTER_AUTH_TOKEN:-}"
    depends_on:
      - redis
      - browserless

  browserless:
    image: browserless/chrome:latest
    restart: always
    ulimits:
      core:
        hard: 0
        soft: 0

  redis:
    image: redis:alpine
    restart: always
    volumes:
      - redis-data:/data

volumes:
  redis-data:
